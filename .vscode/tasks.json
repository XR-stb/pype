{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: clang++ build for debug",
            "command": "bash",
            "args": ["build_dev.sh"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "isDefault": true,
                "kind": "build"
            }
        }
    ]
}